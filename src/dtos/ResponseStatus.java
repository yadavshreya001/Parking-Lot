package dtos;

/**
 * Enum to represent the status of a response in the Parking Lot.
 * It defines standard constants for indicating the result of an operation.
 */
public enum ResponseStatus {
    SUCCESS,       // Indicates that the operation was successful
    FAILURE,      // Indicates that the operation failed
}